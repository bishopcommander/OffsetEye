# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Environment variables — never commit secrets
.env
.env.local
.env.*.local

# Build output
client/dist/
client/build/
server/dist/

# Uploads (raw PDFs stored on disk)
server/uploads/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# IDE
.vscode/
.idea/

# ML artifacts (trained model JSON export — commit only intentionally)
server/services/ml_model/
*.pkl
*.pickle

# Test coverage
coverage/

# Compressed archives & partial downloads (GitHub rejects files >100MB)
*.zip
*.tar
*.gz
*.rar
*.7z
*.crdownload

# Files exceeding GitHub's 100 MB hard limit
# Discovery_report.pdf is 182 MB (downloadable from Equinor Volve Data Village)
Volve_Reports/Reports/Discovery_report.pdf
# EDM.XML contains Volve F.edm.xml which is 211 MB
**/EDM.XML/
**/*.edm.xml

# WITSML Realtime drilling data (18.0 GB / 20,087 files - exceeds GitHub repo capacity)
# Refer to DATASETS.md for full source download instructions
Volve_WITSML Realtime drilling data/
